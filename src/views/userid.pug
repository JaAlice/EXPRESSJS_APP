extends index.pug 

block main
        form(action="/userid" method="POST")
            p User ID:
            input(type="number" name="User_ID" required=true min="100")
            br
            input(type="submit")
        form(action="/userid" method="POST")
            p Email:
            input(type="text" name="email" required=true)
            br
            input(type="submit")
            br
            div= answer
            div#message= message
            button.button1#button_users
                a(href='/users') Back to Users

block scripts
    script
        include ./scripts/users_script.js